import React from "react";

const RatingTrigger = ({ id }: { id: string }) => {
  return (
    <div className="font-satoshi w-fit cursor-pointer rounded-md border border-zinc-100/[0.1] bg-blue-900 px-5 py-2 text-lg font-semibold">
      Watch now
    </div>
  );
};

export default RatingTrigger;
